main :: () -> s64 {
    asm (
        "movq %0, %%rdi\n"
        :
        : "r" (code)
        : "rax", "rdi"
    );

    asm volatile (
        "movq %0, %%rdi\n"
        "movq $60, %%rax\n"
        "syscall\n"
        :
        : "r" (code)
        : "rax", "rdi"
    );

    ret z;
}
